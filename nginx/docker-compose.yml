services:
  nginx:
    image: nginx:latest
    container_name: nginx
    restart: unless-stopped

    ports:
      - "8088:80"
      - "8443:443"

    volumes:
      - ./conf.d:/etc/nginx/conf.d
      - ./certs:/etc/ssl/private

    networks:
      - proxy

networks:
  proxy:
    external: true
